import React, { useReducer } from 'react';

// declaring useReducer hook
const [state, dispatch] = useReducer(reducer, initialArg, init);

function App() {

    return(
        <div>

        </div>
    )
}